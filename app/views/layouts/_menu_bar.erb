<% if @current_user %>
				<% if @current_user.username == 'admin' || @current_user.username == 'cns' %>
				<div id="menu_bar">
				<b>
				<% if @current_user.username == 'admin' %>
				 <%= link_to "Urządzenia", devices_path %>
				<% else %>
				 <%= link_to "Urządzenia", switch_index_path %>
				<% end %>
				 <%= link_to "Head Endy", hes_path %>
				</b>
				</div>
				<% end %>	
<% end %>
